# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Encryption key for storing Canvas tokens (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=your_fernet_key_here

# Canvas LMS base URL (must be https)
CANVAS_BASE_URL=https://canvas.nus.edu.sg

# SQLite database path (optional)
# DB_PATH=bot.db
